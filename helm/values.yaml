# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: "sergant-ua/kbot"
  tag: "v1.0.5-380039f"
  arch: ""
  os: ""
secret:
  name: "kbot"
  key: "token"
  env: "TELE_TOKEN"
