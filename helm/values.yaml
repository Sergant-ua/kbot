# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: sergantua
  tag: "v1.0.5-15a5001"
  arch: "amd64"
  os: "linux"
secret:
  name: "kbot"
  key: "token"
  env: "TELE_TOKEN"
